[project]
name = "scoring-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "divyanshuzeitstoneai", email = "divyanshu@zeitstoneai.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fpdf2>=2.8.8",
    "pandas>=3.0.6",
    "plotly>=7.1.0",
    "streamlit>=1.64.0",
]

[project.scripts]
scoring-engine = "scoring_engine:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
